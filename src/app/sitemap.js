import { phones } from "@/lib/demo-data";export default function sitemap(){const base=process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000";return ["","/phones","/about"].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path?"weekly":"daily",priority:path?0.8:1})).concat(phones.map(phone=>({url:`${base}/phones/${phone._id}`,lastModified:new Date(phone.createdAt),changeFrequency:"weekly",priority:.7})))}
